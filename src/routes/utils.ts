export enum Path {
    Home = "/",
    Menu = "/meniu",
    About = "/despre-noi",
    Contact = "/contact",
    NotFound = "/404",
    // PrivacyPolicy = "/privacy-policy",
    // TermsAndConditions = "/terms-and-conditions",
}
